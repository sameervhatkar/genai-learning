def chai_flavor(flavor="masala"):
    """Return the flavor of chai."""
    return flavor

print(chai_flavor.__doc__)      #    """Return the flavor of chai."" this line as to be there at first inside the function
print(chai_flavor.__name__)